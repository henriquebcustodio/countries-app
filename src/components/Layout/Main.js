import styled from "styled-components";

const StyledMain = styled.main`
    display: flex;
    align-items: center;
`;

const Main = () => {
    return (
        <StyledMain>

        </StyledMain>
    );
};

export default Main;
